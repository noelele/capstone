<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="noelanthony.com.lostandfoundfinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginANDregister.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginANDregister.registerActivity" />
        <activity
            android:name=".HamburgerFIles.newsFeedActivity"
            android:label="@string/title_activity_news_feed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewsFeed.SubmitLost.submitLostItemActivity" />
        <activity
            android:name=".NewsFeed.SubmitLost.lostConfirmPopUp"
            android:theme="@style/AppTheme.PopMe" />
    </application>

</manifest>